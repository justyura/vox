server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # the api keeps its own /api/v1 prefix, so pass the path through untouched
    location /api/ {
        proxy_pass http://api:8081;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
